services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: recovery_radar
    ports:
      - "5432:5432"
    volumes:
      - recovery-radar-postgres:/var/lib/postgresql/data

volumes:
  recovery-radar-postgres:
