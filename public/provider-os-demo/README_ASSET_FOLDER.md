# AAC Asset Folder

This folder is intentionally separate from the React component.

Use:
- images_flat/ for your 800 PNG image pack or future AAC images.
- generated_svg/ for replaceable placeholder image slots generated from the 4,300+ manifest.

If you replace a placeholder with a real image, keep the same filename or update `aac_image_manifest_4300.json`.
